    #include "SurfelGridHash.ush"

    // RWByteAddressBuffer RWSurfelHashKeyBuf;

    bool SurfelGridHashEntry::acquire() 
    {
        uint prev_value;
        SurfelHashKeyBuf.InterlockedCompareExchange(idx * sizeof(uint), 0, checksum, prev_value);
        return prev_value == 0;
    }